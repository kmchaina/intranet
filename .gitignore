*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Development helper patterns (narrowed)
# NOTE: Tests and phpunit.xml are now tracked for CI. Broad patterns removed.
# If you need to ignore experimental throwaway scripts, prefix them with scratch_ or WIP_ and add patterns below.
# Example (currently commented out):
# scratch_*.php

# Retained (safe) dev scratch patterns (fine-grained). Comment out if needed.
*debug*.php
*backup*
*_backup*
*_new*
*_old*
*_demo*
*_enhanced*

# Logs and backups
/storage/logs/*
/storage/backups/*

# IDE files
*.swp
*.swo
*~
